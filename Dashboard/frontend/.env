# Local development — points directly to the FastAPI dev server.
# For production, Vite uses .env.production automatically during `npm run build`.
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_KEY=
