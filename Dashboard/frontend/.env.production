# Production — Vite bakes these into the JS bundle at build time.
# The base URL must NOT include a trailing slash or /api/v1.
# Axios in client.ts strips trailing slashes; each API module appends /api/v1/...
VITE_API_BASE_URL=https://ngnx-fawryplus.fawrypayments.com:9110

VITE_API_KEY=Nas2Al_allah_Elkhalas@fawry
